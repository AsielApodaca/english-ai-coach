# Secrets
.env
.env.*

# Personal practice data (learner history, profile, tmp audio)
data/

# Downloaded models (whisper STT + piper TTS voices), fetched by scripts/setup.sh
models/

# Node
node_modules/
*.log

# OS / editor
.DS_Store
*.swp

# TypeScript build artifacts (none expected; native type stripping)
dist/