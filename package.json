{
  "name": "english-ai-coach",
  "version": "0.1.0",
  "private": true,
  "description": "Local web app to practice technical English with free LLMs: repeat-after-me chunks, evaluation, progress memory and adaptive next steps.",
  "type": "module",
  "engines": {
    "node": ">=23.4"
  },
  "scripts": {
    "start": "node --watch src/server.ts",
    "start:no-watch": "node src/server.ts",
    "setup": "bash scripts/setup.sh",
    "check": "node --check src/server.ts && node --check src/lib/practice.ts && node --check src/lib/learner.ts && node --check src/lib/storage.ts && node --check src/lib/whisper.ts && node --check src/lib/align.ts && node --check src/lib/piper.ts && node --check src/lib/edge-tts.ts && node --check src/lib/providers/index.ts && node --check public/speech/stt-pick.js",
    "test": "node --test \"tests/*.test.ts\""
  },
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^26.0.0",
    "typescript": "^5.6.0"
  }
}